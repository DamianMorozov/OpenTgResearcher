<common:TgPageBase
    x:Class="OpenTgResearcherDesktop.Views.TgChatDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:common="using:OpenTgResearcherDesktop.Common"
    xmlns:converters="using:OpenTgResearcherDesktop.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:OpenTgResearcherDesktop.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:messages="using:TgStorage.Domain.Messages"
    xmlns:users="using:TgStorage.Domain.Users"
    xmlns:viewmodels="using:OpenTgResearcherDesktop.ViewModels"
    xmlns:views="using:OpenTgResearcherDesktop.Views"
    d:DataContext="{d:DesignInstance Type=viewmodels:TgChatDetailsViewModel}"
    mc:Ignorable="d">

    <common:TgPageBase.Resources>
        <!--  Messages data template  -->
        <DataTemplate x:Key="MessagesViewTemplate" x:DataType="messages:TgEfMessageDto">
            <StackPanel
                Grid.Column="{x:Bind Direction, Converter={StaticResource EnumToColumnConverter}}"
                Grid.ColumnSpan="{x:Bind Direction, Converter={StaticResource EnumToColumnSpanConverter}}"
                Height="Auto"
                Margin="4"
                Padding="10,0,0,10"
                HorizontalAlignment="Stretch"
                Background="{ThemeResource SystemColorBackgroundBrush}"
                CornerRadius="10"
                Opacity="1">
                <!--  Created | Id | Link  -->
                <StackPanel Orientation="Horizontal">
                    <!--  ID  -->
                    <TextBlock
                        x:Uid="TextBlockCreated"
                        Margin="5,5,5,0"
                        Style="{ThemeResource BodyTextBlockStyle}" />
                    <!--  Sensitive data  -->
                    <TextBlock
                        Margin="15,5,5,0"
                        Text="{x:Bind SensitiveData, Mode=OneWay}"
                        Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                    <!--  DateTime changed  -->
                    <TextBlock
                        Margin="15,5,5,0"
                        HorizontalAlignment="Left"
                        Style="{ThemeResource BodyTextBlockStyle}"
                        Text="{x:Bind DtChangedString, Mode=OneWay}"
                        Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <!--  ID  -->
                    <TextBlock
                        x:Uid="TextBlockId"
                        Margin="15,5,5,0"
                        Style="{ThemeResource BodyTextBlockStyle}" />
                    <!--  Sensitive data  -->
                    <TextBlock
                        Margin="10,0,5,0"
                        Text="{x:Bind SensitiveData, Mode=OneWay}"
                        Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                    <TextBlock
                        Margin="10,5,5,0"
                        Style="{ThemeResource BodyTextBlockStyle}"
                        Text="{x:Bind Id, Mode=OneWay}"
                        Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <!--  Copy button  -->
                    <Button
                        Margin="5,0,0,0"
                        HorizontalContentAlignment="Left"
                        Command="{Binding ViewModel.ClipboardSilentWriteCommand}"
                        CommandParameter="{x:Bind Id, Mode=OneWay}"
                        CornerRadius="10">
                        <SymbolIcon Symbol="Copy" />
                    </Button>
                    <!--  Link  -->
                    <TextBlock
                        x:Uid="TextBlockLink"
                        Margin="15,5,5,0"
                        Style="{ThemeResource BodyTextBlockStyle}" />
                    <!--  Sensitive data  -->
                    <TextBlock
                        Margin="10,0,5,0"
                        Text="{x:Bind SensitiveData, Mode=OneWay}"
                        Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                    <TextBlock
                        Margin="10,5,5,0"
                        Style="{ThemeResource BodyTextBlockStyle}"
                        Text="{x:Bind Link, Mode=OneWay}"
                        Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <!--  Copy button  -->
                    <Button
                        Margin="5,0,0,0"
                        HorizontalContentAlignment="Left"
                        Command="{Binding ViewModel.ClipboardSilentWriteCommand}"
                        CommandParameter="{x:Bind Link, Mode=OneWay}"
                        CornerRadius="10">
                        <SymbolIcon Symbol="Copy" />
                    </Button>
                </StackPanel>
                <!--  Message  -->
                <StackPanel Orientation="Horizontal">
                    <TextBlock
                        x:Uid="TextBlockMessage"
                        Margin="5,0,5,0"
                        Style="{ThemeResource BodyTextBlockStyle}" />
                    <!--  Sensitive data  -->
                    <TextBlock
                        Margin="10,0,5,0"
                        Text="{x:Bind SensitiveData, Mode=OneWay}"
                        Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                    <TextBlock
                        Margin="10,0,5,0"
                        Style="{ThemeResource BodyTextBlockStyle}"
                        Text="{x:Bind MessageText, Mode=OneWay}"
                        Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <!--  Copy button  -->
                    <Button
                        Margin="5,-5,0,0"
                        HorizontalContentAlignment="Left"
                        Command="{Binding ViewModel.ClipboardSilentWriteCommand}"
                        CommandParameter="{x:Bind MessageText, Mode=OneWay}"
                        CornerRadius="10">
                        <SymbolIcon Symbol="Copy" />
                    </Button>
                </StackPanel>
                <!--  Sender  -->
                <StackPanel Orientation="Horizontal">
                    <TextBlock
                        x:Uid="TextBlockSender"
                        Margin="5,0,5,0"
                        Style="{ThemeResource BodyTextBlockStyle}" />
                    <!--  Sensitive data  -->
                    <TextBlock
                        Margin="10,0,5,0"
                        Text="{x:Bind SensitiveData, Mode=OneWay}"
                        Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                    <TextBlock
                        Margin="10,0,5,0"
                        Style="{ThemeResource BodyTextBlockStyle}"
                        Text="{x:Bind UserId, Mode=OneWay}"
                        Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <!--  Copy button  -->
                    <Button
                        Margin="5,-5,0,0"
                        HorizontalContentAlignment="Left"
                        Command="{Binding ViewModel.ClipboardSilentWriteCommand}"
                        CommandParameter="{x:Bind UserId, Mode=OneWay}"
                        CornerRadius="10">
                        <SymbolIcon Symbol="Copy" />
                    </Button>
                </StackPanel>
                <!--  Image preview  -->
                <Image
                    MaxHeight="1000"
                    Margin="0,10,0,10"
                    Source="{x:Bind ImageFullPath, Mode=OneWay, Converter={StaticResource SafeImagePathConverter}}"
                    Stretch="Uniform"
                    Tapped="OnImagePreviewClick"
                    Visibility="{x:Bind IsImageExists, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <!--  Message deleted  -->
                <TextBlock
                    x:Uid="TextBlockMessageDeleted"
                    Margin="5,0,5,0"
                    Style="{ThemeResource BodyTextBlockStyle}"
                    Visibility="{x:Bind IsDeleted, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </StackPanel>
        </DataTemplate>
        <!--  Participants data template  -->
        <DataTemplate x:Key="ParticipantsViewTemplate" x:DataType="users:TgEfUserDto">
            <StackPanel
                Height="Auto"
                Margin="4"
                Padding="10,0,0,10"
                HorizontalAlignment="Stretch"
                Background="{ThemeResource SystemColorBackgroundBrush}"
                CornerRadius="10"
                Opacity="1">
                <Button
                    Margin="-5,5,5,-5"
                    HorizontalAlignment="Stretch"
                    Click="OnChatUserClick"
                    CornerRadius="10"
                    Tag="{x:Bind Id, Mode=OneWay}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*" />
                            <RowDefinition Height="1*" />
                            <RowDefinition Height="1*" />
                            <RowDefinition Height="1*" />
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal">
                            <!--  ID  -->
                            <TextBlock
                                x:Uid="TextBlockId"
                                Margin="5,5,5,0"
                                Style="{ThemeResource BodyTextBlockStyle}" />
                            <!--  Sensitive data  -->
                            <TextBlock
                                Margin="5,5,5,0"
                                Text="{x:Bind SensitiveData, Mode=OneWay}"
                                Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                            <TextBlock
                                Margin="10,5,5,0"
                                Style="{ThemeResource BodyTextBlockStyle}"
                                Text="{x:Bind Id, Mode=OneWay}"
                                Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                            <!--  Copy button  -->
                            <Button
                                Margin="5,2,0,0"
                                HorizontalContentAlignment="Left"
                                Command="{Binding ViewModel.ClipboardSilentWriteCommand}"
                                CommandParameter="{x:Bind Id, Mode=OneWay}"
                                CornerRadius="10">
                                <SymbolIcon Symbol="Copy" />
                            </Button>
                            <!--  MainUserName  -->
                            <TextBlock
                                x:Uid="FieldUserName"
                                Margin="20,5,7,2"
                                TextAlignment="Left" />
                            <!--  Sensitive data  -->
                            <TextBlock
                                Margin="5,5,5,0"
                                Text="{x:Bind SensitiveData, Mode=OneWay}"
                                Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                            <TextBlock
                                Margin="5,5,5,0"
                                Style="{ThemeResource BodyTextBlockStyle}"
                                Text="{x:Bind MainUserName, Mode=OneWay}"
                                Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                            <!--  Copy button  -->
                            <Button
                                Margin="5,-2,0,0"
                                HorizontalContentAlignment="Left"
                                Command="{Binding ViewModel.ClipboardSilentWriteCommand}"
                                CommandParameter="{x:Bind MainUserName, Mode=OneWay}"
                                CornerRadius="10">
                                <SymbolIcon Symbol="Copy" />
                            </Button>
                            <!--  AccessHash  -->
                            <TextBlock
                                x:Uid="FieldAccessHash"
                                Margin="20,5,7,2"
                                TextAlignment="Left" />
                            <!--  Sensitive data  -->
                            <TextBlock
                                Margin="5,5,5,0"
                                Text="{x:Bind SensitiveData, Mode=OneWay}"
                                Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                            <TextBlock
                                Margin="5,5,5,0"
                                Style="{ThemeResource BodyTextBlockStyle}"
                                Text="{x:Bind AccessHash, Mode=OneWay}"
                                Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                            <!--  Copy button  -->
                            <Button
                                Margin="5,-2,0,0"
                                HorizontalContentAlignment="Left"
                                Command="{Binding ViewModel.ClipboardSilentWriteCommand}"
                                CommandParameter="{x:Bind AccessHash, Mode=OneWay}"
                                CornerRadius="10">
                                <SymbolIcon Symbol="Copy" />
                            </Button>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Orientation="Horizontal">
                            <!--  IsContactActive  -->
                            <TextBlock
                                x:Uid="FieldIsActive"
                                Margin="5,5,7,2"
                                TextAlignment="Left" />
                            <ToggleSwitch
                                Margin="5,-2,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                IsEnabled="False"
                                IsOn="{x:Bind IsContactActive, Mode=OneWay}" />
                            <!--  IsBot  -->
                            <TextBlock
                                x:Uid="FieldIsBot"
                                Margin="-50,5,7,2"
                                TextAlignment="Left" />
                            <ToggleSwitch
                                Margin="-15,-2,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                IsEnabled="False"
                                IsOn="{x:Bind IsBot, Mode=OneWay}" />
                            <!--  LastSeenAgo  -->
                            <TextBlock
                                x:Uid="FieldLastSeenAgo"
                                Margin="-50,5,7,2"
                                TextAlignment="Left" />
                            <!--  Sensitive data  -->
                            <TextBlock
                                Margin="5,5,5,0"
                                Text="{x:Bind SensitiveData, Mode=OneWay}"
                                Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                            <TextBlock
                                Margin="5,5,5,0"
                                Style="{ThemeResource BodyTextBlockStyle}"
                                Text="{x:Bind LastSeenAgoAsString, Mode=OneWay}"
                                Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                            <!--  Copy button  -->
                            <Button
                                Margin="5,-2,0,0"
                                HorizontalContentAlignment="Left"
                                Command="{Binding ViewModel.ClipboardSilentWriteCommand}"
                                CommandParameter="{x:Bind LastSeenAgoAsString, Mode=OneWay}"
                                CornerRadius="10">
                                <SymbolIcon Symbol="Copy" />
                            </Button>
                            <!--  RestrictionReason  -->
                            <TextBlock
                                x:Uid="FieldRestrictionReason"
                                Margin="20,5,7,2"
                                TextAlignment="Left" />
                            <!--  Sensitive data  -->
                            <TextBlock
                                Margin="5,5,5,0"
                                Text="{x:Bind SensitiveData, Mode=OneWay}"
                                Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                            <TextBlock
                                Margin="5,5,5,0"
                                Style="{ThemeResource BodyTextBlockStyle}"
                                Text="{x:Bind RestrictionReason, Mode=OneWay}"
                                Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                            <!--  Copy button  -->
                            <Button
                                Margin="5,-2,0,0"
                                HorizontalContentAlignment="Left"
                                Command="{Binding ViewModel.ClipboardSilentWriteCommand}"
                                CommandParameter="{x:Bind RestrictionReason, Mode=OneWay}"
                                CornerRadius="10">
                                <SymbolIcon Symbol="Copy" />
                            </Button>
                        </StackPanel>
                        <StackPanel Grid.Row="2" Orientation="Horizontal">
                            <!--  FirstLastName  -->
                            <TextBlock
                                x:Uid="FieldFirstName"
                                Margin="5,0,7,2"
                                TextAlignment="Left" />
                            <!--  Sensitive data  -->
                            <TextBlock
                                Margin="5,5,5,0"
                                Text="{x:Bind SensitiveData, Mode=OneWay}"
                                Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                            <TextBlock
                                Margin="5,0,5,0"
                                Style="{ThemeResource BodyTextBlockStyle}"
                                Text="{x:Bind FirstLastName, Mode=OneWay}"
                                Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                            <!--  Copy button  -->
                            <Button
                                Margin="5,-2,0,0"
                                HorizontalContentAlignment="Left"
                                Command="{Binding ViewModel.ClipboardSilentWriteCommand}"
                                CommandParameter="{x:Bind FirstLastName, Mode=OneWay}"
                                CornerRadius="10">
                                <SymbolIcon Symbol="Copy" />
                            </Button>
                            <!--  PhoneNumber  -->
                            <TextBlock
                                x:Uid="FieldPhoneNumber"
                                Margin="5,0,7,2"
                                TextAlignment="Left" />
                            <!--  Sensitive data  -->
                            <TextBlock
                                Margin="5,5,5,0"
                                Text="{x:Bind SensitiveData, Mode=OneWay}"
                                Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                            <TextBlock
                                Margin="5,0,5,0"
                                Style="{ThemeResource BodyTextBlockStyle}"
                                Text="{x:Bind PhoneNumber, Mode=OneWay}"
                                Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                            <!--  Copy button  -->
                            <Button
                                Margin="5,-2,0,0"
                                HorizontalContentAlignment="Left"
                                Command="{Binding ViewModel.ClipboardSilentWriteCommand}"
                                CommandParameter="{x:Bind PhoneNumber, Mode=OneWay}"
                                CornerRadius="10">
                                <SymbolIcon Symbol="Copy" />
                            </Button>
                            <!--  Status  -->
                            <TextBlock
                                x:Uid="FieldStatus"
                                Margin="5,0,7,2"
                                TextAlignment="Left" />
                            <!--  Sensitive data  -->
                            <TextBlock
                                Margin="5,5,5,0"
                                Text="{x:Bind SensitiveData, Mode=OneWay}"
                                Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                            <TextBlock
                                Margin="5,0,5,0"
                                Style="{ThemeResource BodyTextBlockStyle}"
                                Text="{x:Bind Status, Mode=OneWay}"
                                Visibility="{x:Bind IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                            <!--  Copy button  -->
                            <Button
                                Margin="5,-2,0,0"
                                HorizontalContentAlignment="Left"
                                Command="{Binding ViewModel.ClipboardSilentWriteCommand}"
                                CommandParameter="{x:Bind Status, Mode=OneWay}"
                                CornerRadius="10">
                                <SymbolIcon Symbol="Copy" />
                            </Button>
                        </StackPanel>
                        <StackPanel Grid.Row="3" Orientation="Horizontal">
                            <!--  User deleted  -->
                            <TextBlock
                                x:Uid="TextBlockMessageDeleted"
                                Margin="5,0,5,0"
                                Style="{ThemeResource BodyTextBlockStyle}"
                                Visibility="{x:Bind IsDeleted, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        </StackPanel>
                    </Grid>
                </Button>
            </StackPanel>
        </DataTemplate>
    </common:TgPageBase.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!--  Load data  -->
        <views:TgLoadDataPage
            Grid.Row="0"
            Margin="0,0,0,10"
            Visibility="{x:Bind ViewModel.IsPageLoad, Mode=OneWay}" />
        <Grid Grid.Row="1" Visibility="{x:Bind ViewModel.IsPageLoad, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <!--  Actions  -->
            <ContentControl
                Grid.Row="0"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch">
                <Expander
                    x:Uid="SettingAppActions"
                    Margin="{StaticResource ExpanderMargin}"
                    Padding="{StaticResource ExpanderPadding}"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    IsExpanded="True">
                    <Grid Margin="0,-10,0,-10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>
                        <!--  Update from Telegram  -->
                        <Button
                            Grid.Column="0"
                            Margin="0,0,5,0"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Command="{x:Bind ViewModel.UpdateOnlineCommand}"
                            IsEnabled="{x:Bind ViewModel.IsOnlineReady, Mode=OneWay}"
                            Visibility="{x:Bind ViewModel.IsDownloading, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                            <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                                <FontIcon Margin="0,0,10,0" Glyph="&#xe777;" />
                                <TextBlock
                                    x:Uid="TextBlockUpdateOnline"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <!--  Stop downloading  -->
                        <Button
                            Grid.Column="0"
                            Margin="0,0,5,0"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Command="{x:Bind ViewModel.StopDownloadingCommand}"
                            IsEnabled="{x:Bind ViewModel.IsOnlineReady, Mode=OneWay}"
                            Visibility="{x:Bind ViewModel.IsDownloading, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                                <FontIcon Margin="0,0,10,0" Glyph="&#xE71A;" />
                                <TextBlock
                                    x:Uid="TextBlockStopDownloading"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <!--  Clear viewing  -->
                        <Button
                            Grid.Column="1"
                            Margin="0,0,5,0"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Command="{x:Bind ViewModel.ClearDataStorageCommand}"
                            IsEnabled="{x:Bind ViewModel.IsDownloading, Mode=OneWay, Converter={StaticResource InverseBooleanConverter}}">
                            <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                                <FontIcon Margin="0,0,10,0" Glyph="&#xe894;" />
                                <TextBlock
                                    x:Uid="ActionClearView"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <!--  Save settings  -->
                        <Button
                            Grid.Column="2"
                            Margin="0,0,5,0"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Command="{x:Bind ViewModel.SaveChatSettingsCommand}"
                            IsEnabled="{x:Bind ViewModel.IsDownloading, Mode=OneWay, Converter={StaticResource InverseBooleanConverter}}">
                            <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                                <FontIcon Margin="0,0,10,0" Glyph="&#xe894;" />
                                <TextBlock
                                    x:Uid="TextBlockSaveChatSettings"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                    </Grid>
                </Expander>
            </ContentControl>
            <!--  Data in storage  -->
            <ContentControl
                Grid.Row="1"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                IsEnabled="{x:Bind ViewModel.IsDownloading, Mode=OneWay, Converter={StaticResource InverseBooleanConverter}}">
                <Expander
                    x:Uid="ExpanderDataInStorage"
                    Margin="{StaticResource ExpanderMargin}"
                    Padding="{StaticResource ExpanderPadding}"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    IsExpanded="False">
                    <ScrollView HorizontalScrollMode="Disabled" VerticalScrollMode="Auto">
                        <Grid Margin="0,0,10,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="1*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="1*" />
                            </Grid.ColumnDefinitions>
                            <!--  Uid  -->
                            <TextBlock
                                x:Uid="FieldUid"
                                Grid.Row="0"
                                Margin="2,7,7,2"
                                TextAlignment="Left" />
                            <!--  Sensitive data  -->
                            <TextBlock
                                Grid.Row="0"
                                Grid.Column="1"
                                Margin="2,10,2,2"
                                HorizontalAlignment="Stretch"
                                HorizontalTextAlignment="Left"
                                Text="{x:Bind ViewModel.SensitiveData, Mode=OneWay}"
                                Visibility="{x:Bind ViewModel.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                            <TextBox
                                Grid.Row="0"
                                Grid.Column="1"
                                Margin="2"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Left"
                                IsEnabled="False"
                                Text="{x:Bind ViewModel.Dto.Uid, Mode=OneWay}"
                                Visibility="{x:Bind ViewModel.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                            <!--  DtChanged  -->
                            <TextBlock
                                x:Uid="FieldDtChanged"
                                Grid.Row="0"
                                Grid.Column="2"
                                Margin="12,7,7,2"
                                TextAlignment="Left" />
                            <TextBox
                                Grid.Row="0"
                                Grid.Column="3"
                                Margin="2"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Left"
                                IsEnabled="False"
                                IsReadOnly="True"
                                Text="{x:Bind ViewModel.Dto.DtChangedString, Mode=OneWay}" />
                            <!--  Id  -->
                            <TextBlock
                                x:Uid="FieldId"
                                Grid.Row="1"
                                Grid.Column="0"
                                Margin="2,0,7,2"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Style="{ThemeResource BodyTextBlockStyle}" />
                            <Grid
                                Grid.Row="1"
                                Grid.Column="1"
                                Margin="2"
                                HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <!--  Sensitive data  -->
                                <TextBlock
                                    Grid.Column="0"
                                    Margin="2,10,2,2"
                                    HorizontalAlignment="Stretch"
                                    HorizontalTextAlignment="Left"
                                    Text="{x:Bind ViewModel.SensitiveData, Mode=OneWay}"
                                    Visibility="{x:Bind ViewModel.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                                <TextBox
                                    Grid.Column="0"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Left"
                                    IsEnabled="False"
                                    IsReadOnly="True"
                                    Text="{x:Bind ViewModel.Dto.Id, Mode=OneWay}"
                                    Visibility="{x:Bind ViewModel.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                <!--  Copy  -->
                                <Button
                                    Grid.Column="1"
                                    Margin="10,0,0,0"
                                    HorizontalContentAlignment="Left"
                                    Command="{Binding ViewModel.ClipboardSilentWriteCommand}"
                                    CommandParameter="{x:Bind ViewModel.Dto.Id, Mode=OneWay}"
                                    CornerRadius="10">
                                    <SymbolIcon Symbol="Copy" />
                                </Button>
                            </Grid>
                            <!--  IsRestrictSavingContent  -->
                            <TextBlock
                                x:Uid="FieldIsRestrictSavingContent"
                                Grid.Row="1"
                                Grid.Column="2"
                                Margin="12,10,7,2"
                                TextAlignment="Left" />
                            <ToggleSwitch
                                Grid.Row="1"
                                Grid.Column="3"
                                Margin="2"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                IsEnabled="False"
                                IsOn="{x:Bind ViewModel.Dto.IsRestrictSavingContent, Mode=OneWay}" />
                            <!--  IsActive  -->
                            <TextBlock
                                x:Uid="FieldIsActive"
                                Grid.Row="2"
                                Grid.Column="0"
                                Margin="2,10,7,2"
                                TextAlignment="Left" />
                            <ToggleSwitch
                                Grid.Row="2"
                                Grid.Column="1"
                                Margin="2"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                IsEnabled="False"
                                IsOn="{x:Bind ViewModel.Dto.IsActive, Mode=OneWay}" />
                            <!--  IsAutoUpdate  -->
                            <TextBlock
                                x:Uid="FieldIsAutoUpdate"
                                Grid.Row="2"
                                Grid.Column="2"
                                Margin="12,10,7,2"
                                TextAlignment="Left" />
                            <ToggleSwitch
                                Grid.Row="2"
                                Grid.Column="3"
                                Margin="2"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                IsEnabled="False"
                                IsOn="{x:Bind ViewModel.Dto.IsAutoUpdate, Mode=OneWay}" />
                            <!--  UserName  -->
                            <TextBlock
                                x:Uid="FieldUserName"
                                Grid.Row="3"
                                Grid.Column="0"
                                Margin="2,7,7,2"
                                TextAlignment="Left" />
                            <Grid
                                Grid.Row="3"
                                Grid.Column="1"
                                Grid.ColumnSpan="3"
                                Margin="2"
                                HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox
                                    Grid.Column="0"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Left"
                                    IsEnabled="False"
                                    IsReadOnly="True"
                                    Text="{x:Bind ViewModel.Dto.UserName, Mode=OneWay}" />
                                <!--  Copy  -->
                                <Button
                                    Grid.Column="1"
                                    Margin="10,0,0,0"
                                    HorizontalContentAlignment="Left"
                                    Command="{Binding ViewModel.ClipboardSilentWriteCommand}"
                                    CommandParameter="{x:Bind ViewModel.Dto.UserName, Mode=OneWay}"
                                    CornerRadius="10">
                                    <SymbolIcon Symbol="Copy" />
                                </Button>
                            </Grid>
                            <!--  Title  -->
                            <TextBlock
                                x:Uid="FieldTitle"
                                Grid.Row="4"
                                Grid.Column="0"
                                Margin="2,7,7,2"
                                TextAlignment="Left" />
                            <Grid
                                Grid.Row="4"
                                Grid.Column="1"
                                Grid.ColumnSpan="3"
                                Margin="2"
                                HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox
                                    Grid.Column="0"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Left"
                                    IsEnabled="False"
                                    IsReadOnly="True"
                                    Text="{x:Bind ViewModel.Dto.Title, Mode=OneWay}" />
                                <!--  Copy  -->
                                <Button
                                    Grid.Column="1"
                                    Margin="10,0,0,0"
                                    HorizontalContentAlignment="Left"
                                    Command="{Binding ViewModel.ClipboardSilentWriteCommand}"
                                    CommandParameter="{x:Bind ViewModel.Dto.Title, Mode=OneWay}"
                                    CornerRadius="10">
                                    <SymbolIcon Symbol="Copy" />
                                </Button>
                            </Grid>
                            <!--  About  -->
                            <TextBlock
                                x:Uid="FieldAbout"
                                Grid.Row="5"
                                Grid.Column="0"
                                Margin="2,7,7,2"
                                TextAlignment="Left" />
                            <Grid
                                Grid.Row="5"
                                Grid.Column="1"
                                Grid.ColumnSpan="3"
                                Margin="2"
                                HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox
                                    Grid.Column="0"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Left"
                                    IsEnabled="False"
                                    IsReadOnly="True"
                                    Text="{x:Bind ViewModel.Dto.About, Mode=OneWay}" />
                                <!--  Copy  -->
                                <Button
                                    Grid.Column="1"
                                    Margin="10,0,0,0"
                                    HorizontalContentAlignment="Left"
                                    Command="{Binding ViewModel.ClipboardSilentWriteCommand}"
                                    CommandParameter="{x:Bind ViewModel.Dto.About, Mode=OneWay}"
                                    CornerRadius="10">
                                    <SymbolIcon Symbol="Copy" />
                                </Button>
                            </Grid>
                            <!--  Directory  -->
                            <TextBlock
                                x:Uid="FieldDirectory"
                                Grid.Row="6"
                                Grid.Column="0"
                                Margin="2,7,7,2"
                                TextAlignment="Left" />
                            <Grid
                                Grid.Row="6"
                                Grid.Column="1"
                                Grid.ColumnSpan="3"
                                Margin="2"
                                HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <!--  Sensitive data  -->
                                <TextBlock
                                    Grid.Column="0"
                                    Margin="2,10,2,2"
                                    HorizontalAlignment="Stretch"
                                    HorizontalTextAlignment="Left"
                                    Text="{x:Bind ViewModel.SensitiveData, Mode=OneWay}"
                                    Visibility="{x:Bind ViewModel.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                                <TextBox
                                    Grid.Column="0"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Left"
                                    Text="{x:Bind ViewModel.Dto.Directory, Mode=TwoWay}"
                                    Visibility="{x:Bind ViewModel.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                <!--  Copy  -->
                                <Button
                                    Grid.Column="1"
                                    Margin="10,0,0,0"
                                    HorizontalContentAlignment="Left"
                                    Command="{Binding ViewModel.ClipboardSilentWriteCommand}"
                                    CommandParameter="{x:Bind ViewModel.Dto.Directory, Mode=OneWay}"
                                    CornerRadius="10">
                                    <SymbolIcon Symbol="Copy" />
                                </Button>
                            </Grid>
                            <!--  FirstId  -->
                            <TextBlock
                                x:Uid="FieldFirstId"
                                Grid.Row="7"
                                Grid.Column="0"
                                Margin="2,7,7,2"
                                TextAlignment="Left" />
                            <Grid
                                Grid.Row="7"
                                Grid.Column="1"
                                Margin="2"
                                HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox
                                    Grid.Column="0"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Left"
                                    Text="{x:Bind ViewModel.Dto.FirstId, Mode=TwoWay}" />
                                <!--  Copy  -->
                                <Button
                                    Grid.Column="1"
                                    Margin="10,0,0,0"
                                    HorizontalContentAlignment="Left"
                                    Command="{Binding ViewModel.ClipboardSilentWriteCommand}"
                                    CommandParameter="{x:Bind ViewModel.Dto.FirstId, Mode=OneWay}"
                                    CornerRadius="10">
                                    <SymbolIcon Symbol="Copy" />
                                </Button>
                            </Grid>
                            <!--  Count  -->
                            <TextBlock
                                x:Uid="FieldCount"
                                Grid.Row="7"
                                Grid.Column="2"
                                Margin="12,7,7,2"
                                TextAlignment="Left" />
                            <Grid
                                Grid.Row="7"
                                Grid.Column="3"
                                Margin="2"
                                HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox
                                    Grid.Column="0"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Left"
                                    Text="{x:Bind ViewModel.Dto.Count, Mode=OneWay}" />
                                <!--  Copy  -->
                                <Button
                                    Grid.Column="1"
                                    Margin="10,0,0,0"
                                    HorizontalContentAlignment="Left"
                                    Command="{Binding ViewModel.ClipboardSilentWriteCommand}"
                                    CommandParameter="{x:Bind ViewModel.Dto.Count, Mode=OneWay}"
                                    CornerRadius="10">
                                    <SymbolIcon Symbol="Copy" />
                                </Button>
                            </Grid>
                        </Grid>
                    </ScrollView>
                </Expander>
            </ContentControl>
            <!--  Downloading  -->
            <ContentControl
                Grid.Row="2"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch">
                <Expander
                    x:Uid="ExpanderDownloading"
                    Margin="{StaticResource ExpanderMargin}"
                    Padding="{StaticResource ExpanderPadding}"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    IsEnabled="{x:Bind ViewModel.IsDownloading, Mode=OneWay, Converter={StaticResource InverseBooleanConverter}}"
                    IsExpanded="False">
                    <ScrollView HorizontalScrollMode="Disabled" VerticalScrollMode="Auto">
                        <Grid Margin="0,0,10,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="1*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="1*" />
                            </Grid.ColumnDefinitions>
                            <!--  Threads count  -->
                            <TextBlock
                                x:Uid="FieldCountThreads"
                                Grid.Row="0"
                                Margin="2,7,7,2"
                                TextAlignment="Left" />
                            <NumberBox
                                Grid.Row="0"
                                Grid.Column="1"
                                Margin="2"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Left"
                                LargeChange="10"
                                Maximum="{x:Bind ViewModel.DownloadSettings.LimitThreads, Mode=TwoWay}"
                                Minimum="1"
                                SmallChange="1"
                                SpinButtonPlacementMode="Compact"
                                Value="{x:Bind ViewModel.DownloadSettings.CountThreads, Mode=TwoWay}" />
                            <!--  IsActive  -->
                            <TextBlock
                                x:Uid="FieldIsActive"
                                Grid.Row="0"
                                Grid.Column="2"
                                Margin="12,7,7,2"
                                TextAlignment="Left" />
                            <ToggleSwitch
                                Grid.Row="0"
                                Grid.Column="3"
                                Margin="2"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Left"
                                IsEnabled="False"
                                IsOn="{x:Bind ViewModel.DownloadSettings.IsActive, Mode=OneWay}" />
                            <!--  Overwriting files  -->
                            <TextBlock
                                x:Uid="FieldIsRewriteFiles"
                                Grid.Row="1"
                                Grid.Column="0"
                                Margin="2,7,7,2"
                                TextAlignment="Left" />
                            <ToggleSwitch
                                Grid.Row="1"
                                Grid.Column="1"
                                Margin="2"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Left"
                                IsEnabled="True"
                                IsOn="{x:Bind ViewModel.DownloadSettings.IsRewriteFiles, Mode=TwoWay}" />
                            <!--  Overwriting messages  -->
                            <TextBlock
                                x:Uid="FieldIsRewriteMessages"
                                Grid.Row="1"
                                Grid.Column="2"
                                Margin="12,7,7,2"
                                TextAlignment="Left" />
                            <ToggleSwitch
                                Grid.Row="1"
                                Grid.Column="3"
                                Margin="2"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Left"
                                IsOn="{x:Bind ViewModel.DownloadSettings.IsRewriteMessages, Mode=TwoWay}" />
                            <!--  Joining message ID with file  -->
                            <TextBlock
                                x:Uid="FieldIsJoinFileNameWithMessageId"
                                Grid.Row="2"
                                Grid.Column="0"
                                Margin="2,7,7,2"
                                TextAlignment="Left" />
                            <ToggleSwitch
                                Grid.Row="2"
                                Grid.Column="1"
                                Margin="2"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Left"
                                IsEnabled="True"
                                IsOn="{x:Bind ViewModel.DownloadSettings.IsJoinFileNameWithMessageId, Mode=TwoWay}" />
                            <!--  Creating subdirectories  -->
                            <TextBlock
                                x:Uid="FieldIsCreatingSubdirectories"
                                Grid.Row="2"
                                Grid.Column="2"
                                Margin="12,7,7,2"
                                TextAlignment="Left" />
                            <ToggleSwitch
                                Grid.Row="2"
                                Grid.Column="3"
                                Margin="2"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Left"
                                IsOn="{x:Bind ViewModel.Dto.IsCreatingSubdirectories, Mode=TwoWay}" />
                            <!--  File naming by message subdirectories  -->
                            <TextBlock
                                x:Uid="FieldIsFileNamingByMessage"
                                Grid.Row="3"
                                Grid.Column="2"
                                Margin="12,7,7,2"
                                TextAlignment="Left" />
                            <ToggleSwitch
                                Grid.Row="3"
                                Grid.Column="3"
                                Margin="2"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Left"
                                IsOn="{x:Bind ViewModel.Dto.IsFileNamingByMessage, Mode=TwoWay}" />
                            <!--  StateSourceDt  -->
                            <TextBlock
                                x:Uid="FieldDtChanged"
                                Grid.Row="4"
                                Grid.Column="0"
                                Margin="2,7,7,2"
                                TextAlignment="Left" />
                            <TextBox
                                Grid.Row="4"
                                Grid.Column="1"
                                Margin="2"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Left"
                                IsEnabled="False"
                                Text="{x:Bind ViewModel.StateSourceDt, Mode=OneWay}" />
                            <!--  StateSourceMsg  -->
                            <TextBlock
                                x:Uid="FieldMessage"
                                Grid.Row="4"
                                Grid.Column="2"
                                Margin="12,7,7,2"
                                TextAlignment="Left" />
                            <TextBox
                                Grid.Row="4"
                                Grid.Column="3"
                                Margin="2"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Left"
                                IsEnabled="False"
                                IsReadOnly="True"
                                Text="{x:Bind ViewModel.StateSourceMsg, Mode=OneWay}" />
                            <!--  StateSourceProgressString  -->
                            <TextBlock
                                x:Uid="FieldPercent"
                                Grid.Row="5"
                                Grid.Column="0"
                                Margin="2,7,7,2"
                                TextAlignment="Left" />
                            <TextBox
                                Grid.Row="5"
                                Grid.Column="1"
                                Margin="2"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Left"
                                IsEnabled="False"
                                Text="{x:Bind ViewModel.StateSourceProgressString, Mode=OneWay}" />
                            <!--  StateSourceDirectory  -->
                            <TextBlock
                                x:Uid="FieldDirectory"
                                Grid.Row="5"
                                Grid.Column="2"
                                Margin="12,7,7,2"
                                TextAlignment="Left" />
                            <!--  Sensitive data  -->
                            <TextBlock
                                Grid.Row="5"
                                Grid.Column="3"
                                Margin="2,10,2,2"
                                HorizontalAlignment="Stretch"
                                HorizontalTextAlignment="Left"
                                Text="{x:Bind ViewModel.SensitiveData, Mode=OneWay}"
                                Visibility="{x:Bind ViewModel.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                            <TextBox
                                Grid.Row="5"
                                Grid.Column="3"
                                Margin="2"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Left"
                                IsEnabled="False"
                                IsReadOnly="True"
                                Text="{x:Bind ViewModel.StateSourceDirectory, Mode=OneWay}"
                                Visibility="{x:Bind ViewModel.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        </Grid>
                    </ScrollView>
                </Expander>
            </ContentControl>
            <!--  Chat participants  -->
            <ContentControl
                Grid.Row="4"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch">
                <Expander
                    x:Uid="ExpanderChatParticipants"
                    Margin="{StaticResource ExpanderMargin}"
                    Padding="{StaticResource ExpanderPadding}"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    IsEnabled="{x:Bind ViewModel.IsDownloading, Mode=OneWay, Converter={StaticResource InverseBooleanConverter}}"
                    IsExpanded="False"
                    Visibility="{x:Bind ViewModel.LicenseService.CurrentLicense.LicenseType, Converter={StaticResource LicenseTypeToVisibilityConverter}, Mode=OneWay}">
                    <Grid Margin="0,0,10,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="1*" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>
                        <!--  Get participants  -->
                        <Button
                            Margin="0,0,5,5"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Command="{x:Bind ViewModel.GetParticipantsCommand}"
                            IsEnabled="{x:Bind ViewModel.IsOnlineReady, Mode=OneWay}">
                            <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                                <FontIcon Margin="0,0,10,0" Glyph="&#xe777;" />
                                <TextBlock
                                    x:Uid="TextBlockGetParticipants"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <!--  List view with participants  -->
                        <ListView
                            x:Name="ListViewParticipants"
                            Grid.Row="1"
                            MaxHeight="1000"
                            ItemTemplate="{StaticResource ParticipantsViewTemplate}"
                            ItemsSource="{x:Bind ViewModel.UserDtos, Mode=OneWay}">
                            <!--  The ItemsStackPanel must be edited to update the following two properties  -->
                            <!--  These properties allow the list to grow from the bottom  -->
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsStackPanel VerticalAlignment="Top" ItemsUpdatingScrollMode="KeepLastItemInView" />
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                            <!--  This setter property is created so ListViewItems correctly align to the left or right.  -->
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                </Style>
                            </ListView.ItemContainerStyle>
                        </ListView>
                    </Grid>
                </Expander>
            </ContentControl>
            <!--  Chat statistics  -->
            <ContentControl
                x:Name="ContentControlChatStatistics"
                Grid.Row="5"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch">
                <Expander
                    x:Uid="ExpanderChatStatistics"
                    Margin="{StaticResource ExpanderMargin}"
                    Padding="{StaticResource ExpanderPadding}"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    IsEnabled="{x:Bind ViewModel.IsDownloading, Mode=OneWay, Converter={StaticResource InverseBooleanConverter}}"
                    IsExpanded="False"
                    Visibility="{x:Bind ViewModel.LicenseService.CurrentLicense.LicenseType, Converter={StaticResource LicenseTypeToVisibilityConverter}, Mode=OneWay}">
                    <Grid Margin="0,0,10,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="1*" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>
                        <!--  Statistics calculation button  -->
                        <Button
                            Margin="0,0,5,5"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Command="{x:Bind ViewModel.CalcChatStatisticsCommand}"
                            IsEnabled="{x:Bind ViewModel.IsOnlineReady, Mode=OneWay}">
                            <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                                <FontIcon Margin="0,0,10,0" Glyph="&#xe777;" />
                                <TextBlock
                                    x:Uid="TextBlockCalcChatStatistics"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <!--  ChatStatisticsDto  -->
                        <Grid Grid.Row="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="1*" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="1*" />
                            </Grid.ColumnDefinitions>
                            <!--  DtStart  -->
                            <TextBlock x:Uid="TextBlockDtStart" VerticalAlignment="Center" />
                            <DatePicker
                                Grid.Column="1"
                                Margin="10,5,0,10"
                                HorizontalAlignment="Left"
                                SelectedDate="{x:Bind ViewModel.ChatStatisticsDto.DtStart, Mode=TwoWay}" />
                            <!--  DtEnd  -->
                            <TextBlock
                                x:Uid="TextBlockDtEnd"
                                Grid.Row="1"
                                Grid.Column="0"
                                VerticalAlignment="Center" />
                            <DatePicker
                                Grid.Row="1"
                                Grid.Column="1"
                                Margin="10,01,0,10"
                                SelectedDate="{x:Bind ViewModel.ChatStatisticsDto.DtEnd, Mode=TwoWay}" />
                            <!--  User and bot statistics  -->
                            <TextBlock
                                x:Uid="TextBlockUsers"
                                Grid.Row="0"
                                Grid.RowSpan="2"
                                Grid.Column="2"
                                Margin="50,0,0,0"
                                VerticalAlignment="Center"
                                Text="" />
                            <TextBlock
                                Grid.Row="0"
                                Grid.RowSpan="2"
                                Grid.Column="3"
                                Margin="10,0,0,0"
                                VerticalAlignment="Center"
                                Text="{x:Bind ViewModel.ChatStatisticsDto.UsersCount, Mode=OneWay}" />
                            <TextBlock
                                x:Uid="TextBlockBots"
                                Grid.Row="0"
                                Grid.RowSpan="2"
                                Grid.Column="4"
                                Margin="20,0,0,0"
                                VerticalAlignment="Center" />
                            <TextBlock
                                Grid.Row="0"
                                Grid.RowSpan="2"
                                Grid.Column="5"
                                Margin="10,0,0,0"
                                VerticalAlignment="Center"
                                Text="{x:Bind ViewModel.ChatStatisticsDto.BotsCount, Mode=OneWay}" />
                            <!--  List of users and number of messages (UserDtos)  -->
                            <ListView
                                Grid.Row="2"
                                Grid.ColumnSpan="7"
                                MaxHeight="1000"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Stretch"
                                ItemsSource="{x:Bind ViewModel.ChatStatisticsDto.UserWithCountDtos}">
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="users:TgEfUserWithCountDto">
                                        <StackPanel
                                            Padding="5"
                                            Orientation="Horizontal"
                                            Spacing="10">
                                            <!--  Id  -->
                                            <TextBlock x:Uid="FieldId" Margin="20,0,0,0" />
                                            <!--  Sensitive data  -->
                                            <TextBlock
                                                Margin="0,5,5,0"
                                                Text="{x:Bind UserDto.SensitiveData, Mode=OneWay}"
                                                Visibility="{x:Bind UserDto.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                                            <!--  Id  -->
                                            <TextBlock
                                                Width="Auto"
                                                Margin="0,0,0,0"
                                                Text="{x:Bind UserDto.Id}"
                                                Visibility="{x:Bind UserDto.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                            <!--  Copy button  -->
                                            <Button
                                                Margin="5,-2,0,0"
                                                Command="{Binding ViewModel.ClipboardSilentWriteCommand}"
                                                CommandParameter="{x:Bind UserDto.Id, Mode=OneWay}"
                                                CornerRadius="10">
                                                <SymbolIcon Symbol="Copy" />
                                            </Button>
                                            <!--  FirstLastName  -->
                                            <TextBlock x:Uid="FieldFirstName" Margin="20,0,0,0" />
                                            <!--  Sensitive data  -->
                                            <TextBlock
                                                Margin="0,5,5,0"
                                                Text="{x:Bind UserDto.SensitiveData, Mode=OneWay}"
                                                Visibility="{x:Bind UserDto.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                                            <!--  FirstLastName  -->
                                            <TextBlock
                                                Margin="0,0,0,0"
                                                Text="{x:Bind UserDto.FirstLastName}"
                                                Visibility="{x:Bind UserDto.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                            <!--  Copy button  -->
                                            <Button
                                                Margin="5,-2,0,0"
                                                Command="{Binding ViewModel.ClipboardSilentWriteCommand}"
                                                CommandParameter="{x:Bind UserDto.FirstLastName, Mode=OneWay}"
                                                CornerRadius="10">
                                                <SymbolIcon Symbol="Copy" />
                                            </Button>
                                            <!--  Count  -->
                                            <TextBlock x:Uid="FieldMessagesCount" Margin="20,0,0,0" />
                                            <TextBlock
                                                Margin="5,0,0,0"
                                                FontWeight="SemiBold"
                                                Text="{x:Bind Count}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>
                    </Grid>
                </Expander>
            </ContentControl>
            <!--  Content with messages and media  -->
            <ContentControl
                Grid.Row="6"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                Visibility="{x:Bind ViewModel.IsEnabledContent, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Expander
                    x:Uid="ExpanderContent"
                    Margin="{StaticResource ExpanderMargin}"
                    Padding="{StaticResource ExpanderPadding}"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    IsExpanded="False">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="1*" />
                        </Grid.RowDefinitions>
                        <!--  Empty data  -->
                        <TextBlock
                            x:Uid="FieldEmptyData"
                            Grid.Row="0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="{x:Bind ViewModel.EmptyData, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        <!--  Data  -->
                        <ScrollView
                            Grid.Row="1"
                            Margin="0,0,10,0"
                            HorizontalScrollMode="Disabled"
                            VerticalScrollMode="Auto"
                            Visibility="{x:Bind ViewModel.EmptyData, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="1*" />
                                    <RowDefinition Height="1*" />
                                </Grid.RowDefinitions>
                                <!--  List view with messages  -->
                                <ListView
                                    x:Name="ListViewMessages"
                                    MaxHeight="1000"
                                    ItemTemplate="{StaticResource MessagesViewTemplate}"
                                    ItemsSource="{x:Bind ViewModel.MessageDtos, Mode=OneWay}"
                                    Visibility="{x:Bind ViewModel.IsImageViewerVisible, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                    <!--  The ItemsStackPanel must be edited to update the following two properties  -->
                                    <!--  These properties allow the list to grow from the bottom  -->
                                    <ListView.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <ItemsStackPanel VerticalAlignment="Top" ItemsUpdatingScrollMode="KeepLastItemInView" />
                                        </ItemsPanelTemplate>
                                    </ListView.ItemsPanel>
                                    <!--  This setter property is created so ListViewItems correctly align to the left or right.  -->
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                </ListView>
                                <!--  Full-size image on top of ListView  -->
                                <Border
                                    Grid.Row="1"
                                    Grid.RowSpan="2"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch"
                                    Background="#CC000000"
                                    IsHitTestVisible="{x:Bind ViewModel.IsImageViewerVisible, Mode=OneWay}">
                                    <Image
                                        x:Name="FullScreenImage"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Stretch="Uniform"
                                        Tapped="OnFullScreenImageTapped"
                                        Visibility="{x:Bind ViewModel.IsImageViewerVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                </Border>
                            </Grid>
                        </ScrollView>
                    </Grid>
                </Expander>
            </ContentControl>
            <!--  Content statistics  -->
            <ContentControl
                Grid.Row="7"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                Visibility="{x:Bind ViewModel.IsEnabledContent, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Expander
                    x:Uid="ExpanderContentStatistics"
                    Margin="{StaticResource ExpanderMargin}"
                    Padding="{StaticResource ExpanderPadding}"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    IsExpanded="False">
                    <Grid Margin="0,0,10,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="1*" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>
                        <!--  Statistics calculation button  -->
                        <Button
                            Margin="0,0,5,5"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Command="{x:Bind ViewModel.CalcContentStatisticsCommand}"
                            IsEnabled="{x:Bind ViewModel.IsOnlineReady, Mode=OneWay}">
                            <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                                <FontIcon Margin="0,0,10,0" Glyph="&#xe777;" />
                                <TextBlock
                                    x:Uid="TextBlockCalcContentStatistics"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <!--  ContentStatisticsDto  -->
                        <Grid Grid.Row="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <!--  Files statistics  -->
                            <TextBlock
                                x:Uid="TextBlockImages"
                                Grid.Row="0"
                                Grid.Column="2"
                                Margin="50,0,0,0"
                                VerticalAlignment="Center"
                                Text="" />
                            <TextBlock
                                Grid.Row="0"
                                Grid.Column="3"
                                Margin="10,0,0,0"
                                VerticalAlignment="Center"
                                Text="{x:Bind ViewModel.ContentStatisticsDto.ImagesCount, Mode=OneWay}" />
                            <TextBlock
                                x:Uid="TextBlockAudios"
                                Grid.Row="0"
                                Grid.Column="4"
                                Margin="50,0,0,0"
                                VerticalAlignment="Center" />
                            <TextBlock
                                Grid.Row="0"
                                Grid.Column="5"
                                Margin="10,0,0,0"
                                VerticalAlignment="Center"
                                Text="{x:Bind ViewModel.ContentStatisticsDto.AudiosCount, Mode=OneWay}" />
                            <TextBlock
                                x:Uid="TextBlockVideos"
                                Grid.Row="0"
                                Grid.Column="5"
                                Margin="50,0,0,0"
                                VerticalAlignment="Center" />
                            <TextBlock
                                Grid.Row="0"
                                Grid.Column="6"
                                Margin="10,0,0,0"
                                VerticalAlignment="Center"
                                Text="{x:Bind ViewModel.ContentStatisticsDto.VideosCount, Mode=OneWay}" />
                        </Grid>
                    </Grid>
                </Expander>
            </ContentControl>
            <SelectorBar Grid.Row="8" SelectionChanged="SelectorBar_SelectionChanged">
                <SelectorBarItem x:Uid="SelectorBarItemPageChatDetails" />
                <!--<SelectorBarItem x:Uid="SelectorBarItemPageChatParticipants" />
                <SelectorBarItem x:Uid="SelectorBarItemPageChatStatistics" />
                <SelectorBarItem x:Uid="SelectorBarItemPageContent" />
                <SelectorBarItem x:Uid="SelectorBarItemPageContentStatistics" />-->
            </SelectorBar>
            <Frame
                x:Name="ContentFrame"
                Grid.Row="9"
                IsNavigationStackEnabled="False" />
        </Grid>
    </Grid>
</common:TgPageBase>