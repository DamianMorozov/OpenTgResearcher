<common:TgPageBase
    x:Class="OpenTgResearcherDesktop.Views.TgSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:common="using:OpenTgResearcherDesktop.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:OpenTgResearcherDesktop.Helpers"
    xmlns:l="using:WinUI3Localizer"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:styles="using:OpenTgResearcherDesktop.Styles"
    xmlns:viewmodels="using:OpenTgResearcherDesktop.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:TgSettingsViewModel}"
    mc:Ignorable="d">

    <ScrollView VerticalScrollMode="Auto">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <!--  Actions  -->
            <Expander
                Grid.Row="0"
                Margin="{StaticResource ExpanderMargin}"
                Padding="{StaticResource ExpanderPadding}"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                l:Uids.Uid="ExpanderActions"
                IsExpanded="True">
                <Grid Margin="0,-10,0,-10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <!--  Load button  -->
                    <Button
                        Margin="0,0,5,0"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Command="{x:Bind Path=ViewModel.SettingsLoadCommand}">
                        <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                            <FontIcon Margin="10,0,20,0" Glyph="&#xE896;" />
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                l:Uids.Uid="ActionLoad" />
                        </StackPanel>
                    </Button>
                    <!--  Default button  -->
                    <Button
                        Grid.Column="1"
                        Margin="0,0,5,0"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Command="{x:Bind Path=ViewModel.SettingsDefaultCommand}">
                        <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                            <FontIcon Margin="10,0,20,0" Glyph="&#xE72C;" />
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                l:Uids.Uid="ActionResetToDefault" />
                        </StackPanel>
                    </Button>
                    <!--  Save button  -->
                    <Button
                        Grid.Column="2"
                        Margin="5,0,0,0"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Command="{x:Bind Path=ViewModel.SettingsSaveCommand}">
                        <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                            <FontIcon Margin="10,0,20,0" Glyph="&#xE74E;" />
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                l:Uids.Uid="ActionSave" />
                        </StackPanel>
                    </Button>
                </Grid>
            </Expander>
            <!--  App  -->
            <Expander
                Grid.Row="1"
                Margin="{StaticResource ExpanderMargin}"
                Padding="{StaticResource ExpanderPadding}"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                l:Uids.Uid="ExpanderSettingApp"
                IsExpanded="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <!--  Language  -->
                    <TextBlock
                        Grid.Column="0"
                        Margin="10,5,10,5"
                        l:Uids.Uid="SettingAppLanguage"
                        TextAlignment="Left"
                        ToolTipService.ToolTip="{x:Bind helpers:TgResourceExtensions.GetSettingAppLanguageTooltip}" />
                    <StackPanel
                        Grid.Column="1"
                        Width="20"
                        Margin="5"
                        HorizontalAlignment="Center" />
                    <ComboBox
                        x:Name="ComboBoxAppLanguages"
                        Grid.Column="2"
                        Margin="5"
                        HorizontalAlignment="Stretch"
                        ItemsSource="{x:Bind ViewModel.SettingsService.AppLanguages, Mode=OneWay}"
                        SelectedItem="{x:Bind ViewModel.SettingsService.AppLanguage, Mode=TwoWay}" />
                    <!--  Theme  -->
                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="0"
                        Margin="10,5,10,5"
                        l:Uids.Uid="SettingAppTheme"
                        TextAlignment="Left"
                        ToolTipService.ToolTip="{x:Bind helpers:TgResourceExtensions.GetSettingAppThemeTooltip}" />
                    <StackPanel
                        Grid.Column="1"
                        Width="20"
                        Margin="5"
                        HorizontalAlignment="Center" />
                    <ComboBox
                        x:Name="ComboBoxAppThemes"
                        Grid.Row="1"
                        Grid.Column="2"
                        Margin="5"
                        HorizontalAlignment="Stretch"
                        ItemsSource="{x:Bind ViewModel.SettingsService.AppThemes, Mode=OneWay}"
                        SelectedItem="{x:Bind ViewModel.SettingsService.AppTheme, Mode=TwoWay}" />
                </Grid>
            </Expander>
            <!--  SettingReadOnly  -->
            <Expander
                Grid.Row="2"
                Margin="{StaticResource ExpanderMargin}"
                Padding="{StaticResource ExpanderPadding}"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                l:Uids.Uid="SettingReadOnly"
                IsExpanded="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <!--  User directory  -->
                    <TextBlock
                        Margin="10,5,10,5"
                        l:Uids.Uid="SettingUserDirectory"
                        TextAlignment="Left" />
                    <!--  Sensitive data  -->
                    <TextBlock
                        Grid.Column="2"
                        Margin="5"
                        Text="{x:Bind ViewModel.SensitiveData, Mode=OneWay}"
                        Visibility="{x:Bind ViewModel.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}" />
                    <TextBlock
                        Grid.Column="2"
                        Margin="15,5,5,5"
                        HorizontalAlignment="Stretch"
                        Text="{x:Bind ViewModel.UserDirectory, Mode=OneWay}"
                        Visibility="{x:Bind ViewModel.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <styles:TgAnimatedClipboardButton
                        Grid.Column="3"
                        Click="OnClipboardSilentWriteClick"
                        Style="{StaticResource ClipboardAnimatedButtonStyle}"
                        Tag="{x:Bind ViewModel.UserDirectory, Mode=OneWay}" />
                    <!--  Setting file  -->
                    <TextBlock
                        Grid.Row="1"
                        Margin="10,5,10,5"
                        l:Uids.Uid="SettingFile"
                        TextAlignment="Left" />
                    <!--  Sensitive data  -->
                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="2"
                        Margin="5"
                        Text="{x:Bind ViewModel.SensitiveData, Mode=OneWay}"
                        Visibility="{x:Bind ViewModel.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}" />
                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="2"
                        Margin="15,5,5,5"
                        HorizontalAlignment="Stretch"
                        Text="{x:Bind ViewModel.SettingsService.SettingFile, Mode=OneWay}"
                        Visibility="{x:Bind ViewModel.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <styles:TgAnimatedClipboardButton
                        Grid.Row="1"
                        Grid.Column="3"
                        Click="OnClipboardSilentWriteClick"
                        Style="{StaticResource ClipboardAnimatedButtonStyle}"
                        Tag="{x:Bind ViewModel.SettingsService.SettingFile, Mode=OneWay}" />
                    <!--  Application directory  -->
                    <TextBlock
                        Grid.Row="2"
                        Margin="10,5,10,5"
                        l:Uids.Uid="SettingAppDirectory"
                        TextAlignment="Left" />
                    <!--  Sensitive data  -->
                    <TextBlock
                        Grid.Row="2"
                        Grid.Column="2"
                        Margin="5"
                        Text="{x:Bind ViewModel.SensitiveData, Mode=OneWay}"
                        Visibility="{x:Bind ViewModel.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}" />
                    <TextBlock
                        Grid.Row="2"
                        Grid.Column="2"
                        Margin="15,5,5,5"
                        HorizontalAlignment="Stretch"
                        Text="{x:Bind ViewModel.SettingsService.ApplicationDirectory, Mode=OneWay}"
                        Visibility="{x:Bind ViewModel.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <styles:TgAnimatedClipboardButton
                        Grid.Row="2"
                        Grid.Column="3"
                        Click="OnClipboardSilentWriteClick"
                        Style="{StaticResource ClipboardAnimatedButtonStyle}"
                        Tag="{x:Bind ViewModel.SettingsService.ApplicationDirectory, Mode=OneWay}" />
                </Grid>
            </Expander>
            <!--  SettingRecording  -->
            <Expander
                Grid.Row="3"
                Margin="{StaticResource ExpanderMargin}"
                Padding="{StaticResource ExpanderPadding}"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                l:Uids.Uid="SettingRecording"
                IsExpanded="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <!--  EF storage  -->
                    <TextBlock
                        Grid.Row="0"
                        Margin="10,5,10,5"
                        l:Uids.Uid="SettingStorage"
                        TextAlignment="Left" />
                    <StackPanel
                        Grid.Row="0"
                        Grid.Column="1"
                        Width="40"
                        Margin="5"
                        HorizontalAlignment="Center">
                        <CheckBox
                            Margin="10,0,0,0"
                            IsChecked="{x:Bind ViewModel.SettingsService.IsExistsAppStorage, Mode=OneWay}"
                            IsEnabled="False" />
                    </StackPanel>
                    <!--  Sensitive data  -->
                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="2"
                        Margin="5,0,0,0"
                        Text="{x:Bind ViewModel.SensitiveData, Mode=OneWay}"
                        Visibility="{x:Bind ViewModel.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}" />
                    <TextBox
                        Grid.Row="0"
                        Grid.Column="2"
                        Margin="5,0,0,0"
                        HorizontalAlignment="Stretch"
                        Text="{x:Bind ViewModel.SettingsService.AppStorage, Mode=TwoWay}"
                        Visibility="{x:Bind ViewModel.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="3"
                        MinWidth="60"
                        Margin="5,0,0,0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Text="{x:Bind ViewModel.SettingsService.AppStorageFileSize, Mode=OneWay}" />
                    <styles:TgAnimatedClipboardButton
                        Grid.Row="0"
                        Grid.Column="4"
                        Click="OnClipboardSilentWriteClick"
                        Style="{StaticResource ClipboardAnimatedButtonStyle}"
                        Tag="{x:Bind ViewModel.SettingsService.AppStorage, Mode=OneWay}" />
                    <!--  Session  -->
                    <TextBlock
                        Grid.Row="1"
                        Margin="10,5,10,5"
                        l:Uids.Uid="SettingSession"
                        TextAlignment="Left" />
                    <StackPanel
                        Grid.Row="1"
                        Grid.Column="1"
                        Width="40"
                        Margin="5"
                        HorizontalAlignment="Center">
                        <CheckBox
                            Margin="10,0,0,0"
                            IsChecked="{x:Bind ViewModel.SettingsService.IsExistsAppSession, Mode=OneWay}"
                            IsEnabled="False" />
                    </StackPanel>
                    <!--  Sensitive data  -->
                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="2"
                        Margin="5,0,0,0"
                        Text="{x:Bind ViewModel.SensitiveData, Mode=OneWay}"
                        Visibility="{x:Bind ViewModel.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}" />
                    <TextBox
                        Grid.Row="1"
                        Grid.Column="2"
                        Margin="5,0,0,0"
                        HorizontalAlignment="Stretch"
                        Text="{x:Bind ViewModel.SettingsService.AppSession, Mode=TwoWay}"
                        Visibility="{x:Bind ViewModel.IsDisplaySensitiveData, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="3"
                        MinWidth="60"
                        Margin="5,0,0,0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Text="{x:Bind ViewModel.SettingsService.AppSessionFileSize, Mode=OneWay}" />
                    <styles:TgAnimatedClipboardButton
                        Grid.Row="1"
                        Grid.Column="4"
                        Click="OnClipboardSilentWriteClick"
                        Style="{StaticResource ClipboardAnimatedButtonStyle}"
                        Tag="{x:Bind ViewModel.SettingsService.AppSession, Mode=OneWay}" />
                    <!--  Warning  -->
                    <FontIcon
                        Grid.Row="2"
                        Grid.RowSpan="3"
                        Grid.Column="1"
                        Glyph="&#xE7BA;" />
                    <TextBlock
                        Grid.Row="2"
                        Grid.Column="2"
                        Margin="5,0,0,0"
                        VerticalAlignment="Stretch"
                        l:Uids.Uid="FieldSettingsFileWarning1"
                        TextWrapping="WrapWholeWords" />
                    <TextBlock
                        Grid.Row="3"
                        Grid.Column="2"
                        Margin="5,0,0,0"
                        VerticalAlignment="Stretch"
                        l:Uids.Uid="FieldSettingsFileWarning2"
                        TextWrapping="WrapWholeWords" />
                    <TextBlock
                        Grid.Row="4"
                        Grid.Column="2"
                        Margin="5,0,0,0"
                        VerticalAlignment="Stretch"
                        l:Uids.Uid="FieldSettingsFileWarning3"
                        TextWrapping="WrapWholeWords" />
                </Grid>
            </Expander>
        </Grid>
    </ScrollView>
</common:TgPageBase>
