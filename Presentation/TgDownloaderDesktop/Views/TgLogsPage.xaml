<Page
	x:Class="TgDownloaderDesktop.Views.TgLogsPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:viewmodels="using:TgDownloaderDesktop.ViewModels"
	d:DataContext="{d:DesignInstance Type=viewmodels:TgLogsViewModel}"
	mc:Ignorable="d">

	<ScrollView VerticalScrollMode="Auto">
		<ContentControl HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>
				<Expander
					x:Uid="ExpanderUpdate"
					Margin="{StaticResource ExpanderMargin}"
					HorizontalAlignment="Stretch"
					HorizontalContentAlignment="Stretch"
					IsExpanded="True">
					<StackPanel>
						<!--  Update from file  -->
						<Button
							Margin="0,-10,5,0"
							HorizontalAlignment="Stretch"
							VerticalAlignment="Stretch"
							Command="{x:Bind ViewModel.UpdateFromFileCommand, Mode=OneWay}">
							<StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
								<FontIcon Margin="0,0,10,0" Glyph="&#xe777;" />
								<TextBlock
									x:Uid="ActionUpdateFromFile"
									HorizontalAlignment="Center"
									VerticalAlignment="Center" />
							</StackPanel>
						</Button>
						<!--  Errors logs  -->
						<ListView ItemsSource="{x:Bind ViewModel.CollectionLogs, Mode=OneWay}">
							<ListView.ItemTemplate>
								<DataTemplate>
									<TextBlock
										Margin="0,-5,0,-5"
										Style="{StaticResource BodyTextBlockStyle}"
										Text="{Binding}"
										TextWrapping="Wrap" />
								</DataTemplate>
							</ListView.ItemTemplate>
						</ListView>
					</StackPanel>
				</Expander>
			</Grid>
		</ContentControl>
	</ScrollView>
</Page>
